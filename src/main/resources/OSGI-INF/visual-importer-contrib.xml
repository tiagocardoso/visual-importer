<?xml version="1.0"?>
<component name="pt.inevo.nuxeo.threed.filemanager">
  <extension
      target="org.nuxeo.ecm.platform.filemanager.service.FileManagerService"
      point="plugins">
    <plugin name="VisualImporterPlugin"
            class="org.nuxeo.workshop.visualimporter.VisualImporter"
            order="20">
      <filter>image/gif</filter>
      <filter>image/png</filter>
      <filter>image/jpeg</filter>
      <filter>image/pjpeg</filter>
      <filter>image/x-portable-bitmap</filter>
      <filter>image/x-bitmap</filter>
      <filter>image/bmp</filter>
      <filter>image/tiff</filter>
    </plugin>
  </extension>
</component>
